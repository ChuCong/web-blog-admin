import type { Media } from "@/model"
export interface AboutMsd {
    id: number
    content: string
    lang: string
    link_youtube: string
    url_media:string
    type: string
    created_at: string
    updated_at: string
}
